<% content_for(:page_title, "Add New Photo") %>
<div class="row row-cards">
  <div class="col-12">
    <div class="card">
      <div class="card-body">
        <%= render 'form', photo: @photo %>
      </div>
    </div>
  </div>
</div>
